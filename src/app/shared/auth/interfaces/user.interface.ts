export interface User {
    sub: string,
    email: string;
    full_name: string;    
    role: string;
    rolekey: string;
    access_token: string;
}

export interface ProfileImg {
    img: string;
} 